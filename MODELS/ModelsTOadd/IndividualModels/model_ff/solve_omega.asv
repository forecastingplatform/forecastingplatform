function f = solve_omega(omega)

beta   = 0.99;
sigma  = 0.2;
nu     = 0.98;
mu     = 0.1;
we     = 0.01;

F1      = normcdf((log(omega)+0.5*sigma^2)/sigma);
F2      = normcdf((log(omega)+0.5*sigma^2)/sigma-sigma);
F1_prim = normpdf((log(omega)+0.5*sigma^2)/sigma)/omega/sigma;

f = (1-omega*(1-F1)-F2)-(1-F1)/(1-F1-mu*omega*F1_prim)*(nu/beta*(1-omega*(1-F1)-F2)+we_d*(omega*(1-F1)+(1-mu)*F2));
 